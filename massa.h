#if !defined(converteMassa)
#define converteMassa

float toneladaPkilo(float medida);
float toneladaPgrama(float medida);
float kiloPtonelada(float medida);
float kiloPgrama(float medida);
float gramaPtonelada(float medida);
float gramaPkilo(float medida);

#endif // converteMassa